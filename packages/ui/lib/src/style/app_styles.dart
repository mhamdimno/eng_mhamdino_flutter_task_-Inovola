import 'package:flutter/material.dart';

class AppCorners {
  static double mc = 2;
  static BorderRadius mcBorder = BorderRadius.all(mcRadius);
  static Radius mcRadius = Radius.circular(mc);
  static double sm = 4;
  static BorderRadius smBorder = BorderRadius.all(smRadius);
  static Radius smRadius = Radius.circular(sm);
  static double md = 10;
  static BorderRadius mdBorder = BorderRadius.all(mdRadius);
  static Radius mdRadius = Radius.circular(md);
  static double lg = 20;
  static BorderRadius lgBorder = BorderRadius.all(lgRadius);
  static Radius lgRadius = Radius.circular(lg);
  static double xlg = 26;
  static BorderRadius xlgBorder = BorderRadius.all(xlgRadius);
  static Radius xlgRadius = Radius.circular(xlg);

}

class Fonts {
  static const String raleway = "Raleway";
  static const String fraunces = "Fraunces";
}
